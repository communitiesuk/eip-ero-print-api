cloud:
  aws:
    region:
      static: eu-west-2
    credentials:
      access-key: test
      secret-key: test
    sqs:
      endpoint: http://replaced-by-localStackContainerSqsSettings-bean
sqs:
  send-application-to-print-queue-name: send-application-to-print

api:
  ero-management:
    url: http://will-be-replaced-by-wireMockServer-bean

dynamodb:
  print-details-table-name: print-details
  scheduler-lock-table-name: scheduler-locks

jobs:
    print-requests:
        name: BatchPrintRequests
        cron: 0 0 */1 * * *
